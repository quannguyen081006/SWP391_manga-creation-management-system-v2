package manga.enums.chaptertask;

// Chapter/task enum group: mirrors PageTask.status values, including delayed/overdue/cancelled states.
public enum TaskStatus {
    PENDING, IN_PROGRESS, SUBMITTED, APPROVED, REJECTED, OVERDUE
}

